<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lhz.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".preActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoteEditActivity" />
        <activity android:name=".NoteMainActivity" />
        <activity android:name=".NoteMenuActivity" />
        <activity android:name=".Activity2048" />
        <activity android:name=".calculateActivity" />
        <activity android:name=".weatherActivity" />
        <activity android:name=".musicActivity" />
        <activity android:name=".pinballActivity" />
        <activity android:name=".EnglishActivity" />
        <activity android:name=".mobilephoneActivity" />
        <activity android:name=".DeliveryActivity"></activity>
    </application>

</manifest>